# v2.0.9-Eldersign — tag-cut checklist

Status: **staged, not cut.** Codename decided (`Eldersign`). Release blocked on the
cross-version testnet soak signing off; do not cut during the feature-freeze
(no consensus merges to `main` between h=998,000 and h=1,050,667, per #20).

## Timing anchors

- **Soak** (`tncompat` ↔ `rc3`, `~/bin/testnet-soak-status`) must sign off clean first.
- **Freeze-end / bipsoft → main merge:** h=1,050,667 (~2026-07-19).
- **Consensus activation:** h=1,055,555 (~2026-07-23).
- Community release is safe to cut any time once the soak passes — every new rule
  is height-gated to 1,055,555, so the binary behaves identically until then.
  Want adoption runway, so don't cut *late*.

## The 5-file version-string set (apply at cut)

Current is `2.0.8.7`; three files lag at `2.0.8.6` (not rolled for 2.0.8.7) and
jump straight to `2.0.9.0`. Target `getinfo` version int = **2000900**.

| # | File | Change |
|---|---|---|
| 1 | `configure.ac:5-6` | `_CLIENT_VERSION_REVISION 8 → 9`, `_CLIENT_VERSION_BUILD 7 → 0` |
| 2 | `src/version.cpp:15` | `CLIENT_VERSION_SUFFIX "-Restoration" → "-Eldersign"` |
| 3 | `share/setup.nsi:8` | `!define VERSION 2.0.8.6 → 2.0.9.0` |
| 4 | `share/qt/Info.plist` | `CFBundleGetInfoString` (line ~20) `2.0.8.6, … → 2.0.9.0, …` + `CFBundleShortVersionString` (line ~23) → `2.0.9.0` |
| 5 | `qa/pull-tester/build-tests.sh:25` | `DISTDIR=Offerings-2.0.8.6 → Offerings-2.0.9.0` |

Tag suffix and `CLIENT_VERSION_SUFFIX` MUST agree (`-Eldersign`) — see
`feedback-version-suffix-codename-leak`. `Eldersign` confirmed unused (prior:
Bokrug, Triune, -Restoration suffix).

## Then

1. Fill `⟨DATE⟩` + `⟨short-sha⟩` in both drafts:
   - `research/v2.0.9-release-notes.md` (GitHub release body)
   - `research/bct-v2.0.9-release-announcement.txt` (BCT thread 294383 post)
2. CI builds artifacts on tag push (attached as draft release); human publishes
   via `gh api --method PATCH …/releases/<id>` (no `gh release edit` on vps3 —
   see `reference-gh-release-publish-via-patch`).
3. Win64: pull from CI `windows-build-depends.yml`, verify Qt 5.15 / GCC 13-posix
   in `strings` before publishing (`feedback-release-win64-from-ci`).

## Editorial note carried in the drafts

Upgrade pitch is framed **honestly as soft forks** (miners MUST upgrade or mine
orphans; validators/exchanges SHOULD, to enforce — but are not forked off).
This is a deliberate departure from the Triune/Restoration "everyone or you fork
off" copy, because that would be false for BIP66/65/maturity. Flagged for sign-off.
